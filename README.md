````markdown
📱 FindClub — Поиск клубов по интересам

````

````

📖 1. Описание проекта

**FindClub** — это мобильное приложение, разработанное на **Flutter**, предназначенное для поиска клубов и тематических организаций.
Все данные о клубах загружаются из удалённой базы данных **Supabase**, а работа пользователя осуществляется локально, без обязательной регистрации.

✨ Возможности:
- 📋 Просмотр списка клубов с подробной информацией.
- 🔍 Поиск клубов по названию и фильтрация по категориям.
- ❤️ Добавление клубов в избранное (локальное сохранение).
- 🗺️ Просмотр расположения клуба на карте (Yandex MapKit).
- 📱 Полноценная внутренняя навигация.

---

🛠 2. Основные функции приложения

🚀 2.1. Загрузочный экран (Splash Screen)
- Инициализация приложения.
- Установка соединения с Supabase.
- Проверка анонимного доступа.

🧭 2.2. Главная навигация (`main_navigation.dart`)
Реализована через **BottomNavigationBar** и включает разделы:
- Лента клубов
- Избранное
- Карта
- Настройки

📰 2.3. Модуль отображения клубов (`clubs_feed.dart`)
- Загрузка списка из Supabase.
- Отображение карточек.
- Фильтрация и поиск.

ℹ️ 2.4. Подробное описание (`club_details.dart`)
- Фото, адрес, контакты, расписание.
- Построение маршрута.
- Управление избранным.

🗺️ 2.5. Модуль карты (`map_screen.dart`)
- Отображение всех клубов на карте через **Yandex MapKit**.
- Навигация к конкретному клубу.
- Обработка ошибок сети.

⭐ 2.6. Избранное (`favorites_screen.dart`)
- Локальное хранилище (**SharedPreferences**).
- Просмотр и удаление сохраненных клубов офлайн.

⚙️ 2.7. Настройки (`settings_screen.dart`)
- Информация о приложении и контакты.
- Очистка кэша/избранного.

---

💻 3. Используемые технологии

| Технология | Назначение |
|:-----------|:------------|
| **Flutter 3.x** | Фреймворк UI |
| **Dart** | Язык программирования |
| **Supabase** | Backend as a Service (БД + API) |
| **Yandex MapKit** | Карты и навигация |
| **SharedPreferences** | Локальное хранение данных |
| **Provider** | State Management |

---

🚀 4. Установка и запуск

4.1. Клонирование репозитория
```bash
git clone [https://github.com/your_repo/find_club.git](https://github.com/your_repo/find_club.git)
cd find_club
````

4.2. Установка зависимостей

```bash
flutter pub get
```

4.3. Настройка окружения

1.  Создайте файл конфигурации (если он не в репозитории) или отредактируйте `lib/config/supabase_config.dart`:

<!-- end list -->

```dart
const String supabaseURL = "ВАШ_URL_SUPABASE";
const String supabaseKey = "ВАШ_ANON_KEY_SUPABASE";
```

2.  **Важно:** Для работы карт убедитесь, что у вас настроен API Key для Yandex MapKit в `android/app/build.gradle` и `ios/Runner/AppDelegate.swift`.

4.4. Запуск

```bash
flutter run
```

-----

📂 5. Структура проекта

```text
lib/
├── main.dart                  # Точка входа
├── splash_screen.dart         # Экран загрузки
├── navigation/
│   └── main_navigation.dart   # Роутинг
├── screens/
│   ├── clubs_feed.dart        # Лента
│   ├── favorites_screen.dart  # Избранное
│   ├── map_screen.dart        # Карта
│   ├── settings_screen.dart   # Настройки
│   └── club_details.dart      # Детали клуба
├── widgets/
│   └── club_card.dart         # UI-компоненты
├── services/
│   ├── supabase_service.dart  # API Supabase
│   └── favorites_service.dart # Локальное хранение
└── theme/
    ├── app_colors.dart        # Цветовая палитра
    └── text_styles.dart       # Стили текста
```

-----

⚠️ 6. Требования

  * Flutter SDK: `>=3.0.0`
  * Dart SDK: `>=3.0.0`
  * Android SDK / Xcode (для iOS)
  * Активное подключение к интернету

-----

👨‍💻 7. Автор проекта

**Унгефук Владислав**

📧 E-mail: [ungefuk.vlad@mail.ru]
🐱 GitHub: [https://github.com/VLadUVV/FindClub]

```
